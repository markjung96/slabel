# 스라밸학원 홈페이지 리뉴얼 — 기틀 문서

작성일: 2026년 8월 3일
관련 문서: [PLAN.md](./PLAN.md) (마케팅/운영 기획), [QUESTIONS.md](./QUESTIONS.md) (원장님 확인 필요 사항)

---

## 0. 역할 분담

| 단계 | 담당 | 산출물 |
|---|---|---|
| **1. 기틀** | 개발자 + AI | 이 문서. 포지셔닝 틀, IA, 콘텐츠 스키마, 디자인 원칙 |
| **2. 기획** | **원장님** | [QUESTIONS.md](./QUESTIONS.md) 답변 + 증거 자산(사진·리포트·사례) |
| **3. 디자인·구현** | 개발자 | 디자인 시스템, 페이지, 컴포넌트, 배포 |

**중요:** 2단계 답변이 오기 전에 3단계를 시작하면 지금과 똑같은 결과가 나온다.
이유는 아래 1장에 있다.

---

## 1. 진단 — 왜 "AI티"가 나는가

### 1-1. 근본 원인은 디자인 실력이 아니다

**할 말이 없는 상태에서 디자인부터 만들었기 때문이다.**

채울 내용이 없으니 템플릿이 빈칸을 대신 채웠다.
실제 학원 사진도, 리포트 실물도, 학생 사례도, 숫자도 없으니
아이콘·그라디언트·번호 워터마크로 지면을 메울 수밖에 없었다.

> **그래서 해결책은 "디자인을 더 잘하는 것"이 아니라
> "디자인할 실물을 먼저 만드는 것"이다.**

이게 역할 분담을 기획 → 디자인 순서로 잡은 이유다.

### 1-2. 코드에서 확인된 템플릿 클리셰 12가지

실제 파일에서 찾은 것들이다. 리뉴얼 시 전부 제거 대상.

| # | 클리셰 | 위치 |
|---|---|---|
| 1 | 영어 eyebrow 라벨 (`Why Choose Us`, `Schedule`) | `intro-cards.tsx`, `timetable-preview.tsx` |
| 2 | 우상단 대형 번호 워터마크 `01 02 03 04` | `intro-cards.tsx:70` |
| 3 | lucide 아이콘 4개 격자 (졸업모자·타이머·상승그래프·사람) | `intro-cards.tsx` |
| 4 | h1 강조어에 그라디언트 텍스트 (`bg-clip-text text-transparent`) | `hero-carousel.tsx:45` |
| 5 | 히어로 배경 radial-gradient 블롭 2개 | `hero-carousel.tsx:14-15` |
| 6 | 모든 섹션에 동일한 fade-in-up + `delay: index * 0.1` | 전 컴포넌트 |
| 7 | 4개 섹션이 전부 `eyebrow → h2 → 부제 → 그리드` 동일 골격 | 홈 전체 |
| 8 | 학원명만 바꾸면 그대로 쓸 수 있는 카피 | `home.ts`, `about.ts`, `programs.ts` |
| 9 | 실제 사진 0장. 블로그 썸네일도 gradient div로 대체 | `blog/page.tsx:47` |
| 10 | "S-Label만의 독자적인" — 독자성 주장만 있고 증거 없음 | `pt-system.ts:6` |
| 11 | PT 5단계가 전부 추상 동사 ("정밀하게 진단합니다") | `pt-system.ts:23-45` |
| 12 | 학원 홈페이지 기본 템플릿 그대로인 메뉴 구성 | `navigation.ts` |

### 1-3. 카피 문제를 한눈에

현재 홈페이지에서 강조하는 것:

> 맞춤형 커리큘럼 · 1:1 지도 · 소수정예 · 성적 향상 ·
> 데이터 기반 분석 · 자기주도 학습 · 정기 테스트 · 체계적인 관리

**이 8개는 인근 경쟁 학원이 전부 똑같이 쓰고 있다.**
차별점이라고 적은 것이 실은 업계 공통 문구다.

가진 진짜 무기는 **PT 시스템 + 타이머 관리**인데,
그게 실제로 어떻게 돌아가는지 보여주는 자료가 사이트에 하나도 없다.

---

## 2. 리뉴얼 원칙 5가지

### 원칙 1. 주장하지 말고 보여준다

| 지금 (주장) | 바꿀 것 (증거) |
|---|---|
| "데이터로 관리합니다" | 실제 주간 리포트 이미지 (이름만 가림) |
| "체계적인 학습 관리" | 8주 계획표 실물 사진 |
| "타이머 관리 시스템" | 타이머 화면 + 풀이시간 기록지 사진 |
| "성적이 향상됩니다" | 정답률 62%→81% 실제 그래프 |
| "소수 정예" | 실제 강의실 사진, 좌석 수 |

**규칙: 사진이나 실물로 보여줄 수 없는 강점은 홈페이지에 쓰지 않는다.**

### 원칙 2. 형용사를 숫자와 명사로 바꾼다

```
❌ 체계적인 학습 관리를 통해 꾸준한 성적 향상을 이끌어냅니다
✅ 주 1회 리포트, 4주마다 학부모 상담. 오답은 통과할 때까지 재시험.
```

```
❌ 학생 개개인에게 최적화된 맞춤형 커리큘럼
✅ 첫 수업 전 진단 90분 → 8주 계획표 1장으로 정리해 드립니다
```

**규칙: "체계적/최적화/맞춤형/전문적/독자적/진정한" 이 6개 단어는 금지어로 둔다.**
쓰고 싶어지면, 그 자리에 들어갈 구체적 사실이 없다는 신호다.

### 원칙 3. 학부모의 질문 순서대로 페이지를 배치한다

지금 메뉴는 "우리 소개" 순서다. 학부모가 궁금해하는 순서가 아니다.

### 원칙 4. 사진이 디자인의 8할이다

아이콘·그라디언트·도형으로 채운 자리를 실물 사진으로 바꾸면
디자인을 크게 바꾸지 않아도 "AI티"의 대부분이 사라진다.

필요한 사진 목록은 [QUESTIONS.md](./QUESTIONS.md) 6장에 정리했다.

### 원칙 5. 모션은 히어로 한 곳만

지금은 모든 섹션이 똑같이 fade-in-up 한다. 전부 움직이면 아무것도 강조되지 않고,
"템플릿 돌린 티"만 난다. 히어로 1곳만 남기고 나머지는 정적으로 간다.

---

## 3. 새 정보 구조 (IA)

### 3-1. 현재

```
학원 소개 / PT 시스템 / 시간표 / 프로그램(고등부·중등부) / 커리큘럼 / 블로그 / 상담신청
```

7개 메뉴 전부 "우리는 이런 곳입니다" 구조. 경쟁 학원과 구조가 동일하다.

### 3-2. 제안 — 학부모의 질문 순서

| 순서 | 학부모의 속마음 | 페이지 | 이 페이지의 유일한 목표 |
|---|---|---|---|
| 1 | "우리 애한테 맞는 곳인가?" | **이런 학생을 위한 곳입니다** | 대상이 아닌 학부모를 걸러내기 |
| 2 | "그래서 뭘 어떻게 하는데?" | **8주 동안 일어나는 일** | PT 시스템을 실물로 공개 |
| 3 | "진짜 되긴 하나?" | **결과** | 리포트·사례·후기로 증명 |
| 4 | "얼마고 언제 하는데?" | **반 · 시간 · 비용** | 가격 장벽 미리 제거 |
| 5 | "어떻게 시작하지?" | **무료 진단 상담** | 전환 |

**핵심 변화:** 1번에서 **일부러 걸러낸다.**
"모든 학생 환영"은 아무에게도 안 꽂힌다.
"수학 40~70점, 문제는 푸는데 시험시간이 부족한 학생"이라고 좁히면
해당하는 학부모는 반드시 전화한다.

> 어떤 학생을 주력으로 할지는 [PLAN.md 6장](./PLAN.md)의 A~D안 중에서
> 원장님이 고르셔야 한다. → QUESTIONS.md Q1

### 3-3. 홈 화면 구성

```
1. 히어로     — 포지셔닝 한 문장 + 진단 상담 CTA (모션은 여기만)
2. 대상 명시   — "이런 학생이라면" 3가지 상황 (사진 또는 실제 시험지)
3. 8주 흐름    — 진단 → 계획표 → 주간관리 → 리포트 (각 단계 실물 사진 1장)
4. 결과 증거   — 리포트 실물 + 변화 수치 + 후기 2~3개
5. 강사·공간   — 원장 얼굴·이력, 강의실 사진
6. 위치·문의   — 지도, 전화, 상담 신청
```

**삭제:** "왜 스라밸학원인가요?" 아이콘 4개 격자, 번호 워터마크, 영어 eyebrow

---

## 4. 콘텐츠 스키마 — 기틀의 핵심

**원장님 답변이 오는 대로 바로 꽂을 수 있도록, 데이터 구조를 먼저 정의한다.**
디자인보다 이게 먼저다. 틀이 있으면 내용이 채워지는 순간 사이트가 완성된다.

작업 위치: `src/data/` (기존 구조 유지 — 관심사 분리가 잘 되어 있다)

### 4-1. 신규 타입 (`src/types/index.ts`에 추가 예정)

```ts
/** 3-2의 1번 페이지: 우리가 받는 학생 / 받지 않는 학생 */
export interface TargetProfile {
  id: string
  situation: string      // "문제는 푸는데 시험시간이 부족하다"
  detail: string         // 구체적 증상 서술
  image: string | null   // 실제 시험지·오답노트 사진
}

/** 3-2의 2번 페이지: 8주 동안 실제로 일어나는 일 */
export interface ProgramStep {
  week: string           // "1주차" | "2~7주차" | "8주차"
  title: string          // "진단 90분"
  whatWeDo: string[]     // 원장이 하는 일 (동사로 구체적으로)
  whatStudentGets: string[] // 학생이 받는 것 (실물 명사로)
  artifact: string | null   // 그 주에 나오는 산출물 사진 경로
}

/** 3-2의 3번 페이지: 증거 */
export interface Evidence {
  id: string
  kind: "report" | "case" | "review"
  grade: string          // "중2"
  school: string | null  // "흥덕중" (동의 시에만)
  before: string         // "정답률 62%, 시간 부족 7문항"
  after: string          // "정답률 81%, 시간 부족 3문항"
  period: string         // "8주"
  image: string | null   // 익명 처리한 리포트 실물
  quote: string | null   // 학부모/학생 한 마디
}

/** 4번 페이지: 반과 비용 — 가격 장벽을 미리 제거한다 */
export interface ClassOffer {
  id: string
  target: string         // "중2 내신"
  schedule: string       // "화·목 19:00~22:00"
  minutesPerMonth: number
  monthlyFee: number
  includes: string[]     // "주간 리포트", "오답 재시험", "자습실"
  seatsLeft: number | null
}

/** 5번: 강사 — 얼굴과 이력이 신뢰의 8할이다 */
export interface Instructor {
  name: string
  role: string
  subjects: string[]
  grades: string[]
  career: string[]
  photo: string | null
}
```

### 4-2. 이 스키마가 강제하는 것

`whatStudentGets`, `artifact`, `image`, `before`/`after` 같은 필드는
**추상적인 문장으로는 채울 수 없다.** 실물이나 숫자가 있어야 채워진다.

즉 스키마 자체가 "AI티 나는 카피"를 막는 장치다.
빈칸이 남으면 그 섹션은 렌더링하지 않는다 (이번 수정에서 잡은 원칙과 동일).

---

## 5. 디자인 방향

> 상세 시안은 3단계에서 작업. 여기서는 기틀만 고정한다.

### 5-1. 타이포그래피

- 본문: Pretendard 유지 (이번에 로딩 버그 수정 완료)
- **숫자를 주인공으로.** 이 학원의 정체성은 시간·정답률·문항 수다.
  숫자에 tabular-nums + 큰 웨이트를 주고, 설명은 작게.
- 영어 라벨 전면 제거

### 5-2. 컬러

- 현재 `#22577a`(청록) / `#3b8275`(녹색) — 나쁘지 않지만 "신뢰감 있는 교육 브랜드" 기본값이라 특징이 없다
- **제안: 그라디언트를 전부 제거하고 단색으로.** 대신 강조색 1개만 남겨
  "지금 해야 할 행동"(상담 신청·전화)에만 사용한다.
- 브랜드 컬러 최종 결정은 3단계에서. 로고(SLB) 색과 맞춘다.

### 5-3. 이미지

- 스톡 사진 금지. 실제 학원·실제 자료만 사용
- 리포트·시험지는 이름 마스킹 후 그대로 노출 (가공하지 말 것 — 날것이 신뢰를 준다)
- 사진이 없는 섹션은 **아이콘으로 대체하지 말고 섹션 자체를 뺀다**

### 5-4. 모션

- 히어로 진입 1회만
- 스크롤 기반 fade-in 전면 제거
- 유일한 예외: 숫자 카운트업 (실제 데이터가 있을 때만)

---

## 6. 진행 순서

### Phase 0 — 기틀 (개발자 + AI) · 완료 및 진행 중

- [x] 신뢰 누수 수정 및 배포 준비 (`fix/trust-leaks` 브랜치, 푸시 완료)
- [x] 운영/마케팅 기획서 (`PLAN.md`)
- [x] 리뉴얼 기틀 문서 (이 문서)
- [x] 원장님 확인 요청서 (`QUESTIONS.md`)
- [ ] 콘텐츠 스키마 타입 구현 — **원장님 Q1(주력 타깃) 답변 후 착수**

### Phase 1 — 기획 (원장님) · 2주 예상

- [ ] QUESTIONS.md 답변 (필수 항목만이라도)
- [ ] 사진·리포트·사례 등 증거 자산 수집
- [ ] 주력 타깃 1개 확정

**이 단계가 병목이다.** 답변이 오는 만큼만 사이트가 좋아진다.

### Phase 2 — 디자인·구현 (개발자) · 3~4주 예상

- [ ] 디자인 시스템 (컬러·타이포·컴포넌트)
- [ ] IA 재구성 및 라우팅 변경
- [ ] 페이지별 구현
- [ ] 이미지 저장소 이전 (Vercel Blob) — 관리자 업로드가 현재 프로덕션에서 동작 안 함
- [ ] 반응형·접근성·성능 점검
- [ ] 배포

---

## 7. Phase 2로 넘어가기 전 체크리스트

아래가 채워지지 않은 상태로 디자인을 시작하면 지금과 같은 결과가 나온다.

- [ ] 주력 타깃 학생 1개가 한 문장으로 정해졌다
- [ ] 포지셔닝 한 문장이 확정됐다
- [ ] 주간 리포트 **실물**이 최소 1장 있다
- [ ] 8주 계획표 **실물**이 최소 1장 있다
- [ ] 성적/정답률 변화 사례가 최소 2건 있다
- [ ] 학원 내부 사진이 최소 5장 있다
- [ ] 원장님 얼굴 사진과 이력이 있다
- [ ] 반별 시간표와 수강료가 공개 가능한 형태로 정리됐다

**8개 중 5개 이상이 채워지면 Phase 2를 시작한다.**
그 미만이면 디자인이 아니라 자료 수집을 더 해야 한다.
